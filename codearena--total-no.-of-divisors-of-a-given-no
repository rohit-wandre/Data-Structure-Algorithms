//program to count total number of divisors of a given number
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    for(int i=0;i<n;i++)
    {
         int c=0;
         int t=arr[i];
    for(int i=1;i<=t;i++)
    {
        if(t%i==0)
        {
            c++;
        }
    }
    if(c<4)
    {
        cout<<"NO"<<endl;
    }
    else
    {
        cout<<"YES"<<endl;
    }
}
    return 0;
}
